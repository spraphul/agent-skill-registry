name: Validate registry

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python3 -m pip install -e .
      - run: python3 -m json.tool registry.json >/dev/null
      - run: python3 -m json.tool schemas/skill-entry.schema.json >/dev/null
      - run: agent-skill validate
      - run: agent-skill install hello-agent-skill --agent both --dest /tmp/agent-skill-registry-test --dry-run
